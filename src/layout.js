export const seatLayout = [
    [       //Single Row
      {
        rowId: 'A',
        seatNo: 1,
        isBooked: Boolean(Math.round(Math.random()))
      },
      {
        rowId: 'A',
        seatNo: 2,
        isBooked: Boolean(Math.round(Math.random()))
      },
      {
        rowId: 'A',
        seatNo: 3,
        isBooked: Boolean(Math.round(Math.random()))
      },
      {
        rowId: 'A',
        seatNo: 4,
        isBooked: Boolean(Math.round(Math.random()))
      },
      {
        rowId: 'A',
        seatNo: 5,
        isBooked: Boolean(Math.round(Math.random()))
      },
      {
        rowId: 'A',
        seatNo: 6,
        isBooked: Boolean(Math.round(Math.random()))
      },
      {
        rowId: 'A',
        seatNo: 7,
        isBooked: Boolean(Math.round(Math.random()))
      },
      {
        rowId: 'A',
        seatNo: 8,
        isBooked: Boolean(Math.round(Math.random()))
      },
      {
        rowId: 'A',
        seatNo: 9,
        isBooked: Boolean(Math.round(Math.random()))
      },
      {
        rowId: 'A',
        seatNo: 10,
        isBooked: Boolean(Math.round(Math.random()))
      }
    ],
    [       //Single Row
      {
        rowId: 'B',
        seatNo: 1,
        isBooked: Boolean(Math.round(Math.random()))
      },
      {
        rowId: 'B',
        seatNo: 2,
        isBooked: Boolean(Math.round(Math.random()))
      },
      {
        rowId: 'B',
        seatNo: 3,
        isBooked: Boolean(Math.round(Math.random()))
      },
      {
        rowId: 'B',
        seatNo: 4,
        isBooked: Boolean(Math.round(Math.random()))
      },
      {
        rowId: 'B',
        seatNo: 5,
        isBooked: Boolean(Math.round(Math.random()))
      },
      {
        rowId: 'B',
        seatNo: 6,
        isBooked: Boolean(Math.round(Math.random()))
      },
      {
        rowId: 'B',
        seatNo: 7,
        isBooked: Boolean(Math.round(Math.random()))
      },
      {
        rowId: 'B',
        seatNo: 8,
        isBooked: Boolean(Math.round(Math.random()))
      },
      {
        rowId: 'B',
        seatNo: 9,
        isBooked: Boolean(Math.round(Math.random()))
      },
      {
        rowId: 'B',
        seatNo: 10,
        isBooked: Boolean(Math.round(Math.random()))
      }
    ],
    [       //Single Row
      {
        rowId: 'C',
        seatNo: 1,
        isBooked: Boolean(Math.round(Math.random()))
      },
      {
        rowId: 'C',
        seatNo: 2,
        isBooked: Boolean(Math.round(Math.random()))
      },
      {
        rowId: 'C',
        seatNo: 3,
        isBooked: Boolean(Math.round(Math.random()))
      },
      {
        rowId: 'C',
        seatNo: 4,
        isBooked: Boolean(Math.round(Math.random()))
      },
      {
        rowId: 'C',
        seatNo: 5,
        isBooked: Boolean(Math.round(Math.random()))
      },
      {
        rowId: 'C',
        seatNo: 6,
        isBooked: Boolean(Math.round(Math.random()))
      },
      {
        rowId: 'C',
        seatNo: 7,
        isBooked: Boolean(Math.round(Math.random()))
      },
      {
        rowId: 'C',
        seatNo: 8,
        isBooked: Boolean(Math.round(Math.random()))
      },
      {
        rowId: 'C',
        seatNo: 9,
        isBooked: Boolean(Math.round(Math.random()))
      },
      {
        rowId: 'C',
        seatNo: 10,
        isBooked: Boolean(Math.round(Math.random()))
      }
    ],
  ]